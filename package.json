{
	"dependencies": {
		"husky": "^9.1.7",
		"lint-staged": "^16.1.6"
	},
	"scripts": {
		"prepare": "husky",
		"backend-check": "cd backend && go fmt ./... && golangci-lint run",
		"backend-format": "cd backend && go fmt ./...",
		"backend-lint": "cd backend && golangci-lint run"
	}
}
